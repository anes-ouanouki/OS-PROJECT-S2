# Main project settings.
# Stores paths, delivery options, and alert limits.
# Loaded by main.sh at startup.

# This part defines report and log paths.
# it allows custom folders without changing the code.
REPORT_DIR="./reports"
LOG_DIR="./logs"

# This part holds email and remote delivery settings.
# it is used by the email and ssh modules.
RECIPIENT_EMAIL="kada9anes@gmail.com"
MSMTP_ACCOUNT="gmail"

REMOTE_USER="kadaa"
REMOTE_HOST="127.0.0.1"
REMOTE_REPORT_DIR="/home/kada/reports"

# This part sets alert thresholds and the display limit.
# it controls when alerts trigger.
CPU_ALERT_THRESHOLD=80
DISK_ALERT_THRESHOLD=90
RAM_ALERT_THRESHOLD=85
